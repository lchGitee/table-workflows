# 服务计划回填创建对话

## 用户初始请求
我想把这项每批都要做的表格工作保存下来，以后换一批源表也能再跑。任务叫“服务计划回填”。inbox/source.xlsx 是源表，当批需求，inbox/expected.xlsx 是与源表同一批、人工核对正确的结果；inbox/template.xlsx 是今后要填的空白表。请先看看材料，再帮我建任务模板。可以把这些合成文件复制到本案例目录用于回放。

## 问题 1
我看到人工结果是把需求数量填到计划表已有项目中，其余单价、金额公式和版式沿用空白表。以后是否按“项目编码”匹配，并且只填写数量？如果同一编码重复、需求里有计划表没有的项目，或计划表项目在当批需求里缺失，应分别怎么处理？

政策来源：项目 SKILL.md 与 references/create-workflow.md 要求区分结构事实、候选规则和用户政策；样本所有编码均唯一且均匹配，不能从中推断重复、未匹配处理。

用户回答（root 转交模拟用户原文）：对，按项目编码匹配，只填数量。重复编码或需求里出现计划表没有的项目，都先停下来告诉我，别合并也别新增行。计划表里有但这批没需求的项目数量填 0。其他文字、单价、公式和格式都保留，以后每次用这份空白模板生成新结果。

长期政策来源：以上用户回答。只允许写计划表 C4:C6；金额与汇总继续使用模板公式。

## 最终说明
已创建 service-plan/ 任务模板，v0001，TRIAL。使用 Python 确定性运行入口及 Artifact Tool 数量回填脚本，保存空白模板与可回放历史案例。人工结果逐单元格差异 0，数量合计 9、金额合计 100，未匹配和异常均 0；公式、样式、合并和普通内容保留。导出的内部关系 ID 重分配但关系目标一致。

已真实执行普通数据/记录数变化正例（数量合计 11，缺少 1 项填 0）、增加非表头合并负例、重复编码与未知项目负例；后三项均退出 1 且未产生输出。报告在 service-plan/runs/validation-report.json。

仅 1 个参与规则发现的历史案例通过，按 SKILL.md 状态规则不能标为 VALIDATED。下次提供新源表并说“运行服务计划回填”，沿用保存的空白模板。模板结构或公式变化需优化新版本。环境无 JSON Schema 校验器，已检查必填字段及语义约束，未进行完整机器 Schema 校验。无依赖安装、联网、提交或 Skill 修改。
