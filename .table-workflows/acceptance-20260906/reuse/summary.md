# 第一轮结果

固定运行上轮已生成的 monthly-shipped v3 副本，未修复、升级或放宽契约。7 项实际运行均符合冻结预期：

| 场景 | 实际结果 |
|---|---|
| 原输入基线 | 退出 0；W01=12、W02=1、W03=7 |
| 同一导出器独立重建等价文件 | 退出 0；逐格一致 |
| 记录逆序 | 退出 0；逐格一致 |
| 数据、组别与记录数变化 | 退出 0；W01=3、W02=12、W04=8 |
| 数据区域新增合并单元格 | 退出 1；无输出 |
| 同机含空格的新工作目录 | 退出 0；逐格一致 |
| 隔离副本实现被改动 | 退出 1；无输出 |

独立 XML 读取器比较全部非空单元格，不复用被测计算逻辑。原 Skill、旧模板与源文件共 170 个已快照文件保持原哈希。完整回执见 [results.json](results.json)。4 个输入变体已渲染检查；合并变体为刻意的结构负例。

边界：目录迁移仍使用本机同一依赖安装；不证明新电脑可运行。没有找到 Excel/LibreOffice，跨导出器未测。以上是上轮生成模板的复用结果，不证明新生成的任意模板都会达到同样质量。
