# 本月运行诊断

- 任务模板：月度仓库发货汇总，v0001，TRIAL；保存的历史通过案例数量为 1。
- 实际执行固定命令，进程退出状态为 1；未生成结果。
- 5 个执行产物哈希、版本快照关键字段和完整依赖锁校验均通过。
- 已逐项阅读 Manifest Schema 并检查必填字段与主要语义约束；本地 Python 无 jsonschema，未进行完整 Schema 机器校验。
- 本月原表实际结构指纹：872b65afe0ea46b55b5d8d30548f0ae4d0a108e06962a3e4ccc4be082759d00d。
- 保存的期望结构指纹：a4c85a15637d3dcffcf15ba8263ef547f1659d8948c6e4a3b51d44fdad7d3793。
- 差异仅位于 relationships：Excel 内部关系 Id 及依 Id 排序后的顺序变化；关系的 Type 与 Target 对应相同。其他规范化结构字段相同。
- 当前固定版本把这些内部标识纳入精确指纹，因此在读取业务记录与生成输出前阻断。本次未绕过或修改校验，未改写模板版本或源文件。
- 后续需确认仅本次处理还是升级任务模板供以后复用。若升级，应仅规范化内部关系标识并验证引用关系与目标，保留表头、隐藏行列、公式、对象等有意义结构的阻断检查；不得直接忽略全部 relationships。候选版本应回放已确认历史案例，并验证允许的数据变化及真实结构变化负例。

完整实际执行回执：current-month.receipt.json。
