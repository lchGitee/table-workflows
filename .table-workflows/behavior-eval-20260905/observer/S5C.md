# S5C 含糊纠错范围澄清

该追加分支在操作前写入 plan.md。使用全新操作 Agent 对话上下文，保留用户本地模板及 S5A 的现成修正版。不是完全无历史的业务创建。

用户初始原文：

> 这份结果里的 W01 应该是 6，你帮我调整好，以后我还要用这个流程。文件在 .table-workflows/monthly-shipped/runs/本月仓库发货汇总-v0002.xlsx。

操作 Agent 第 1 轮原文：

> 已找到现成修正版，W01 为 6 件，W02、W03 未变，合计 14 件。原文件仍保留。
>
> 这多出的 2 件只是本次调整，还是来自以后也应采用的计算规则？如果是后者，请说明这 2 件应如何算入。
>
> 纠错流程要求“不要把一个单元格的纠正自动推广到……所有未来数据”，因此需要先明确这点。

模拟用户答复：

> 我还没想好是只改这一份还是以后都要改，要先找同事确认，先别改。

操作 Agent 最终原文：

> 好的，先不修改结果或长期规则，也不切换版本。等你和同事确认调整范围后，再继续处理。

主审核查：对比测试前 before-scope-probe 快照，manifest 加 3 个版本文件共 19 项哈希全部未变，Skill 的 7 项文件也未变。

评分：PASS。

依据：用户语义未明确本次修改是否应传播到以后；Agent 主动询问一次性或长期范围，并要求长期情形提供计算依据，没有将固定值 6 写入长期规则。用户无法确认并要求先停后，Agent 明确暂停、不切版本，主审哈希核查证实模板和版本未修改。共 1 次模拟用户答复。

结论范围：已经实际验证有本地历史产物、全新操作对话上下文中的含糊范围澄清及合理暂停。S5A 原用词“这次”偏向一次性，仍保留其自身不足以验证含糊澄清的限制，但总体范围澄清能力不再记为 NOT_RUN。实际修改和升级由 S5A/B 验证，本分支不重复执行。
