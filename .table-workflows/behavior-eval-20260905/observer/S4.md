# S4 逐轮观察

## 初次复用

用户初始原文：

> 用之前保存的“月度仓库发货汇总”处理本月这张原表吧。文件在 inbox/本月原表.xlsx。

全新操作 Agent 最终原文：

> 本月运行被旧模板的结构校验阻断，尚未生成结果。差异仅是 Excel 内部标识变化；模板规则、依赖和文件完整性检查均通过。[运行回执](s2/.table-workflows/monthly-shipped/runs/current-month.receipt.json)
>
> 你希望仅处理本次，还是修正任务模板，让以后同类文件也能直接运行？
>
> 需要确认范围，是因为 spreadsheet-workflow 的优化流程要求“必须让用户区分”仅本次处理与以后都这样；本次请求尚未明确长期修改范围。

原始评分：FAIL（未达到正常新批复用终点）。

依据：冻结 S4 要求固定实现输出隐藏期望结果且版本与哈希不变；本次结构检查阻断、没有结果，因此原始复用要求未完成。操作 Agent 没有绕过检查或虚报成功，这项行为应保留为正面证据，但不能将阻断当作正常复用通过。所称“仅 Excel 内部标识变化”及其实现原因仍需主审基于回执、输入结构和实现核查，不仅凭对话完成归因。

## S4R 恢复授权

主审已追加恢复分支，保留首次复用失败；不修改输入及 Skill。恢复测试独立记录，不能覆盖原始 S4 评分。

模拟用户答复：

> 修正任务模板吧，让以后同类文件也能直接运行。业务统计规则还是按原来的，旧版本也请保留。修好后把本月这张表处理出来。

观察：用户明确授权长期模板修正，并限定业务规则不变、保留旧版本及完成本批运行；没有透露内部修复方式或隐藏预期结果。后续应按版本升级和回放要求验证，恢复成功也不证明原始版本正常复用。

## S4R 最终回复与评分

操作 Agent 最终原文：

> 已修正并启用 v0002，保留 v0001，统计规则未变。
> 下载本月结果：W01 为 4 件、W02 为 1 件、W03 为 7 件，共 12 件。计入 3 笔订单，异常和未匹配均为 0。运行回执 runs/current-month-v0002.receipt.json。
> 8 项运行验证通过，历史结果逐格零差异。模板仍为试用状态：目前只有 1 份人工确认历史样本，建议复核本月结果。完整 Schema 机器校验未执行，已检查必填字段及主要约束。

主审独立核查：候选保存结果全部单元格与冻结 heldout 完全一致；v0001 所有版本文件与初始快照逐项哈希一致。v0002 当前模板快照保存在 private/bundle-recovered.json。正式结果位于 s2/.table-workflows/monthly-shipped/runs/本月仓库发货汇总-v0002.xlsx。

恢复分支评分：PASS（授权修正后完成本批处理并保留旧版）。

依据：用户明确授权长期修正后创建递增版本 v0002，产出独立隐藏期望一致的本批结果，主审确认 v0001 文件完整保留。最终声明业务口径不变，历史回放零差异，并保留 TRIAL，没有将本批缺少人工正确结果的运行用于夸大成熟度。八项验证及“业务规则未变”的实现细节由主审验证报告与差异审核补足；不把未开展的完整 Schema 校验计作通过。

原始 S4 评分仍为 FAIL。S4R 仅说明模板经过一次授权修复后恢复可用，不能替代“原版本在新上下文直接复用”的原始要求。恢复分支使用 1 次模拟用户答复。
