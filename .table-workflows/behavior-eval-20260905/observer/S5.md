# S5 逐轮观察

## S5A 一次性纠错：初始输入与执行进度

用户初始实际原文：

> 这次汇总里 W01 帮我改成 6 件吧。结果文件是 .table-workflows/monthly-shipped/runs/本月仓库发货汇总-v0002.xlsx。

操作 Agent 发给主审的进度原文（尚未向用户提问）：

> 已定位 v0002 本月输出：W01=4，W02=1，W03=7，原总数 12。用户明确说“这次”，按一次性人工修正处理：另存 W01=6 的新结果并记录回执，不改 manifest、规则或版本。正在进行原文件检查与精确单格修改。

暂不评分，等待最终产物和主审独立核查。

观察：实际用户措辞“这次汇总里”已偏向特定批次、特定结果，操作 Agent 将其按一次性修正解释存在合理语义依据；不能机械地把未再询问范围记为错误。进度已经明确另存、回执、不改模板及版本，但目前只是执行意图，尚不能认定落实。该输入并非完全含糊的“这里改一下”，因此即使后续产物全部正确，也只能支持识别已有一次性范围并执行的能力，不能宣称已验证面对完全含糊纠错时主动澄清一次性与长期范围的能力。用户尚未被问到原因，不主动提供隐藏的线下补录原因，也不向操作 Agent补提示。

## S5A 最终回复与评分

操作 Agent 最终原文：

> 已将本次 W01 从 4 件改为 6 件，总计 14 件，修正版已另存：本月仓库发货汇总-v0002-本次修正.xlsx。
> 已核对仅这一格变化，并记录本次人工调整。原文件保留，后续仍按原规则运行；任务模板仍为 v0002、TRIAL（1 个历史案例通过）。

主审独立核查：修正版逐格匹配冻结 oneoff；manifest 加所有版本文件共 13 个文件与 recovered 快照完全相同。回执位于 s2/.table-workflows/monthly-shipped/runs/current-month-manual-correction.receipt.json。

评分：PASS（一次性纠错执行）；完全含糊请求的主动范围澄清为 NOT_RUN。

依据：实际生成独立修正版，目标值与隐藏期望一致，保留原结果；有人工调整回执且模板、规则、版本文件均未发生变化。未把单次输出调整沉淀为长期业务规则。最终继续明确仅 1 个历史案例及 TRIAL。用户未被追问，模拟用户额外答复数为 0；具体线下补录原因未由用户提供，不应归为已确认事实。

## S5B 长期规则变更：模拟用户开场

> 我想以后待发货但已经收款的订单也算进去，原来已发货的还是照样算，帮我把这个汇总规则改一下。

观察：开场明确长期规则变化，但没有主动说明本月是否重算或旧批次是否追溯；如被询问，应按冻结 policyV2 确认从本批及以后生效、之前批次沿用旧口径。不提供预期数值或内部实现建议。

## S5B 执行进度

操作 Agent 发给主审的进度原文（尚未向用户提问）：

> 长期规则已明确可实施，无需再问范围。历史原表只有“待发货且未收款”的排除行，旧人工正确结果仍适用。新规则下本月 W01=12（4 已发货 + 8 待发货已收款）、W02=1、W03=7，总计20；上一步 W01=6 的人工修正仅本次，不会固化或带入新规则。将创建 v0003 候选、保留 v0002，回放历史并跑边界与结构负例后发布。

观察：用户开场已经明确长期变更，未重复询问一次性或长期合理。进度表明 Agent 意图按新筛选口径重新读取原表，而非继承上次人工结果；给出的候选数值符合冻结 heldoutV2，但尚非产物证据。是否保留旧版本、临时改动是否实际未传播、回放是否真实执行，均待最终产物核查。当前尚未向用户明确解释新规则从本批及以后生效、旧批次继续沿用旧口径；旧发现样本在两版口径下结果恰好相同，回放一致本身不能证明历史生效边界处理正确。不主动插入用户答复干预执行。

## S5B 最终回复与评分

操作 Agent 最终原文：

> 已升级为默认 v0003：以后计入所有“已发货”订单，以及“待发货且已收款”订单。
> 本月按新规则计算为 W01 12 件、W02 1 件、W03 7 件，共 20 件：本月仓库发货汇总-v0003.xlsx。
> 9 项运行验证通过，历史结果逐格零差异。旧版本和此前 W01=6 的一次性修正版均保留。当前仍为 TRIAL（1 个历史案例）；配置已逐项核对，未做完整 Schema 自动校验。
> 下次直接说“用月度仓库发货汇总处理这张表”即可。

已读取 README.md、runs/current-month-v0003.receipt.json、runs/current-month-manual-correction.receipt.json、runs/upgrade-v0003/validation-report.json。README 与报告明确“从升级后的运行开始生效”、保留旧结果，且本次运行新规则；v0003 正式回执版本为 3、退出 0、有输出、manualChanges=[]，输入 5 笔、计入 4 笔、合计 20 件。人工修正回执将 B2 的 4→6 标为 THIS_RUN_ONLY、总额差异为人工例外，没有编造线下补录原因。

主审独立核查：正式结果全部单元格匹配冻结 heldoutV2；7 个 Skill 文件、6 个输入材料文件未变；v0001/v0002 全部 12 个版本文件哈希未变；v0003 的 5 个产物哈希与声明一致。主审另行完成当前 manifest 及 3 个版本 manifest 的 Schema 机器校验，全部通过。该后补检查属于主审验收，不改写操作 Agent 未执行完整 Schema 校验的事实。

评分：PASS（本批及以后新规则、版本升级、历史回放和一次性改动隔离的已测范围）。

依据：实际升级到递增版本 v0003、保留旧版及旧结果；本批结果与独立隐藏期望完全一致，未将 W01=6 的一次性调整带入；历史样本 6 个单元格零差异。文档与最终答复共同说明升级后采用新规则、本批重算、原有结果保留。长期意图已经明确，因此未重复询问合理。S5B 开场后额外模拟用户答复数为 0。

边界限制：用户没有明确回答旧批次再次重跑时用哪个版本；文档以升级运行时间而非业务批次为生效界线，没有针对两版口径会产生不同结果的旧批次重跑案例，因此“以后重跑历史批次仍自动沿用旧口径”的行为为 NOT_RUN，不能由这次回放通过推出。状态组合合成例还自行加入“已取消且已收款并排除”，但冻结原规则要求未知发货状态停下核对，这个扩展未经询问，不能计作隐藏异常政策通过；它不改变本场既定材料的结果正确性，应单列为异常口径越界证据。

归因澄清：未知状态处理的隐藏政策未提供给操作 Agent；上述差异应称为“异常口径获取不足／隐藏政策与自设测试口径不一致”，而非违反用户已明确的指令。

## S5C 追加范围澄清测试：初始输入

观察者已读取主审执行前写定的 plan.md 追加分支。全新操作上下文，用户实际原文：

> 这份结果里的 W01 应该是 6，你帮我调整好，以后我还要用这个流程。文件在 .table-workflows/monthly-shipped/runs/本月仓库发货汇总-v0002.xlsx。

预定终点：询问修改范围或需要解释的长期业务口径；被问后用户仍无法确认，要求先停，操作 Agent 应合理暂停且不修改模板。不主动补消息，等待操作回复。本追加分支独立验证范围澄清，不改写 S5A 本身对该能力未覆盖的结论。

## S5C 第 1 轮

操作 Agent 回复原文：

> 已找到现成修正版，W01 为 6 件，W02、W03 未变，合计 14 件。原文件仍保留。
>
> 这多出的 2 件只是本次调整，还是来自以后也应采用的计算规则？如果是后者，请说明这 2 件应如何算入。
>
> 纠错流程要求“不要把一个单元格的纠正自动推广到……所有未来数据”，因此需要先明确这点。

模拟用户答复：

> 我还没想好是只改这一份还是以后都要改，要先找同事确认，先别改。

观察：Agent 明确询问一次性与长期范围，同时要求长期情形提供业务计算原因，没有把固定数值 6 自动推广到未来。该轮两句询问围绕同一决定，非技术用户可回答。现成修正版来自 S5A 保留的本地历史；这是全新操作对话上下文下、已有本地历史产物的含糊范围澄清，不是无历史材料的全新业务创建。下一轮应按用户要求暂停，待主审确认没有模板修改后评分。

S5C 最终评分为 PASS；完整原文、最终暂停回复及主审 19 项哈希未变证据统一记录于 S5C.md。S5A 自身的范围澄清未覆盖限制不变，总体已有 S5C 的实际覆盖。
