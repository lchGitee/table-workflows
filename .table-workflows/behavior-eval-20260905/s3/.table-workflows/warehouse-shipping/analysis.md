# 按仓库发货汇总：待核对

当前处于规则发现阶段，尚未生成或发布可运行的任务模板，没有通过的历史回放案例。

## 已确认材料

- 源表：当前工作区 `inbox/原表.xlsx`，工作表“订单”，5 列、4 条记录。
- 人工结果：当前工作区 `inbox/我做好的.xlsx`，工作表“汇总”，2 列、2 条记录。
- 用户确认两份文件属于同一批数据，目标是以后重复生成按仓库的发货汇总。
- 两份文件均只有一个可见工作表；未发现公式、合并单元格、隐藏行列、命名区域、表格对象、图表或图片。
- 原始文件仅保留外部引用，未复制或修改。

## 用户已确认口径

只统计发货状态为“已发货”的记录，不看收款状态。

## 阻断项

W01 按源表已发货记录得到 2 件，人工结果为 20 件；W02 源表已发货记录为 3 件，与人工结果相同。

用户尚不确定 W01 的差异原因，需向同事核对，并明确要求先不要替其决定规则。因此不能将 20 视为录入错误，也不能推导出乘以 10、仓库特例或人工调整规则。当前差异未获接受或解决，不得记为回放通过。

## 继续所需信息

等待用户核对 W01 的 20 件来源；如为结果录入错误，需用户明确确认；如为换算或调整，需明确依据、适用范围和所需输入。之后再确定任务模板名称、生成实现并执行历史回放及结构校验。
