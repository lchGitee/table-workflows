# table-workflows 项目说明

## 项目目标

本仓库维护通用的 `spreadsheet-workflow` Agent Skill。它负责引导非技术用户将一张或多张源表与人工确认正确的历史结果表，沉淀为可重复运行、可验证、可升级的表格任务模板。

Skill 只定义流程、产物约定和验证方法，不内置考勤、财务、销售等具体业务规则。具体规则应由使用者的 Agent 根据用户提供的样本推导，并保存在用户自己的 `.table-workflows/<workflow-id>/` 中。

## 对话与测试

- 默认使用中文交流，面向非技术用户说明下一步需要什么，不要求用户先理解脚本或 DSL。
- 当用户说“开始测试”“测试这个 Skill”或提出源表到结果表的重复处理需求时，使用 `$spreadsheet-workflow`。
- 若用户没有指定模式，优先进入“创建模板”模式：依次确认源表、人工正确结果表、可选空白结果模板、字段含义和验收标准。
- 一次只询问当前继续执行所必需的信息；收到文件后先检查结构与数据覆盖情况，再推导规则。
- 不猜测缺失的业务口径，不把单个样本中的偶然值写成通用规则。存在多个合理解释时，让用户确认。
- 测试请求只授权运行和检查，不授权修改 Skill。只有用户明确要求修复或优化 Skill 时，才编辑仓库根目录下的 `SKILL.md`、`references/` 或 `agents/openai.yaml`。
- 不读取或复制相邻项目中的业务规则、真实数据或生成模板，除非用户明确提供并授权使用。

## 数据与产物边界

- 不覆盖用户上传的源表、人工正确结果表或空白模板。
- 将测试生成的任务模板、脚本、DSL、运行报告和差异报告放在当前项目的 `.table-workflows/` 下。
- 默认使用合成测试数据；不要把个人信息、公司敏感数据或生成的业务模板提交到本仓库。
- 第一次通过历史样本并不等于规则已经可靠；至少报告逐单元格差异、关键汇总、未匹配记录和异常情况。
- 更新已有模板时创建新版本并保留旧版本，先回放已确认样本，再建议切换默认版本。

## 修改与验证

- 保持改动小而聚焦，延续现有 `SKILL.md + references + agents/openai.yaml` 结构。
- 通用流程放在 `SKILL.md`；创建、运行、改进和产物格式的细节分别维护在对应 reference 中，避免重复。
- 修改后运行 Skill 校验，并检查 YAML、JSON、Markdown 相对链接和未完成占位符。
- 行为变化必须用真实的创建、再次运行或纠错场景验证；不要只检查文案是否出现。
- 未经用户明确要求，不提交、不推送，也不改变远端仓库状态。
