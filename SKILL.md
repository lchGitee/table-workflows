---
name: spreadsheet-workflow
description: 每周、每月都要重复整理表格？提供原始表格和一份人工做好的结果，帮你梳理并确认处理规则，保存为可重复使用的流程。下次换一批数据，就能按相同规则生成结果。适合考勤表与工资表生成、报表汇总、多表匹配和固定模板填报，也支持调整已有流程；不适合没有复用需求的一次性探索分析。
metadata:
  short-description: 把人工做过的表格整理保存为可复用流程，下次换一批数据就能按相同规则生成结果
---

# 表格任务自动化

帮用户把已经人工做过的表格整理保存为可重复使用的流程，后续用新一批数据生成结果。保存的流程在内部称为“任务模板”。本 Skill 只规定引导流程、产物契约和安全边界，不内置业务规则，也不提供通用表格执行引擎。

## 用场景说明用途

用户还不清楚能做什么时，从下列场景中选一两个贴近需求的例子说明；目标已明确时直接进入对应流程，不要求先选场景。

| 场景 | 用户可以得到什么 |
| --- | --- |
| 考勤表与工资表生成 | 结合打卡记录、请假记录和人员名单，按公司模板生成考勤表；也可结合薪资标准和已确认的计薪规则，生成工资表 |
| 销售报表汇总 | 把各门店或渠道的销售明细，按产品、地区或月份汇总成报表 |
| 订单与回款核对 | 匹配订单表和回款表，列出未匹配记录及金额差异 |
| 多表信息合并 | 按客户编号、员工编号等字段，把分散在不同表里的信息合并起来 |
| 数据清洗与转换 | 重复执行去重、筛选、日期格式统一、分类名称转换等操作 |
| 固定模板填报 | 把每期数据整理后填入指定格式的周报、月报或上报表 |

这些是用途示例，不是预置规则。具体处理方式来自用户的样表和确认；不能仅凭打卡、请假记录推定计薪规则。

## 先判断工作模式

根据用户当前目标选择一种模式。意图已经明确时直接继续，不要让用户重复选择。

- **新建任务模板**：用户希望从历史源表和人工正确结果中总结流程。完整阅读并遵循 [新建任务模板](references/create-workflow.md)。
- **运行任务模板**：用户希望用已经保存的流程处理新一批文件。完整阅读并遵循 [运行任务模板](references/run-workflow.md)。
- **优化任务模板**：用户指出已有结果错误、规则变化或模板变化。完整阅读并遵循 [优化任务模板](references/improve-workflow.md)。
- **管理任务模板**：用户希望查看、重命名、导出、归档或回滚任务模板。只读取 [任务模板协议](references/workflow-bundle.md) 中相关部分，并保持现有版本与审计记录。

用户只说“处理这些表格”且项目里已有任务模板时，先根据文件结构寻找候选；只有唯一可靠候选时才建议使用。没有候选时进入新建模式，多个候选时请用户选择。

## 核心约束

1. 第一次创建时可以让 Agent 分析、推理并生成实现；正常复用时必须运行已固定版本，不得重新猜测或静默改写规则。
2. 自然语言说明可以帮助人理解，但不能成为重复执行的唯一依据。可复用流程必须落成可执行脚本，或落成具备已保存解释器/运行器的 DSL。
3. Skill 目录不保存用户规则。任务模板默认保存在当前工作区的 `.table-workflows/<workflow-id>/`，或用户明确指定的位置。
4. 人工结果是规则发现和回放验证的证据，不是绝对无误的训练标签。矛盾、缺失或偶然相关必须显式暴露给用户。
5. 一个历史正确案例只足以把版本标记为 `TRIAL`。没有历史正确结果时可以保存 `DRAFT`，但不得宣称流程已经稳定。
6. 长期纠错必须创建新版本并回放所有可回放历史案例；一次性纠错不得污染任务模板。
7. 每次运行都必须从本次实际输入和目标模板重新提取规范化结构并计算指纹，再与已保存指纹比较；只读取或哈希已保存的结构描述不算运行时校验。结构或实现不一致时停止正常运行并转入优化模式，不得临时生成兜底规则后继续。
8. 始终生成新结果文件，不覆盖源表、人工正确结果或结果模板。只修改已声明的输出区域，并保护其他普通值、公式、样式和工作簿对象。
9. 原始表格默认留在本地。复制历史案例、结果模板或敏感数据到任务模板目录前，先说明用途并取得用户同意。

## 选择执行产物

优先复用当前环境中已经存在且适合该任务的确定性表格能力，不要为了统一形式重建运行时。

- 已有受限 DSL 及其稳定运行器，而且任务能完整表达时，可以保存 DSL。
- 没有可执行的 DSL 运行器，或任务涉及定制读取、关联、计算、布局和写入时，生成最小的确定性脚本。
- 不要只生成一个 DSL 文件却把解释工作留给下次 Agent；如选择 DSL，解释器或固定运行命令必须同时进入任务模板契约。
- 不限定 Python、JavaScript 或特定表格库。根据用户环境选择，并记录运行命令、依赖和所有执行文件的校验值。使用非标准库或外部运行器时必须保存并校验机器可读的依赖锁；只记录安装路径、符号链接或笼统的运行时名称不算锁定。
- 日常运行不得让模型逐格生成最终业务结果；模型只负责引导、解释异常或在优化模式中产生候选新版本。

## 面向小白的交互

- 优先沿用用户对文件和结果的称呼，如“打卡记录”“公司考勤表”。需要介绍术语时，用“原始表格（源表）”“你已经做对的一份结果表”“公司要求的空白表格”“保存好的整理流程”解释，不要求用户理解坐标、JSON、DSL 或代码。
- 每个阶段用一两句话说明当前要做什么、为什么需要这份材料或确认、接下来会得到什么。已有明确目标或已收到文件时，直接结合当前材料继续，不重复功能介绍或罗列全部场景。
- 一次只请求当前阶段缺少的材料。用户已经上传的文件先检查，不要让其重新上传。
- 先复述识别到的文件角色和目标，再只询问会改变结果的歧义，例如关联键、重复记录、空值、舍入、优先级和特殊例外。
- 给用户展示规则结论、依据、影响范围和未解决项；除非用户主动要求，不展示内部 DSL 或大段代码。
- 完成时先给结果和核对结论，再说明保存的流程、版本、仍需人工关注的内容和下次用法。状态用普通话解释，如“已用一批历史数据核对通过，仍需试用复核”，不要只展示 `TRIAL`、`VALIDATED` 等内部标记，也不要把一次核对通过说成以后都不会出错。

## 任务模板契约

创建、运行或修改任务模板前，读取 [任务模板协议](references/workflow-bundle.md)。`manifest.json` 必须符合 [Manifest JSON Schema](references/manifest.schema.json)。如果当前环境没有 JSON Schema 校验器，至少逐项检查必填字段，并在交付说明中注明未进行机器校验。
